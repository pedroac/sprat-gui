name: CI/CD

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main
      - release

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  prepare:
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'push' ||
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'pull_request' &&
       github.event.pull_request.head.ref != 'main' &&
       github.event.pull_request.head.ref != 'release')
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        run: |
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            BRANCH="${{ github.base_ref }}"
          else
            # Extracts 'release' from 'refs/heads/release'
            BRANCH=$(echo "${{ github.ref }}" | sed 's|refs/heads/||')
          fi
          echo "Detected branch: $BRANCH"
          if [ "$BRANCH" == "release" ]; then
            echo 'matrix=["ubuntu-latest", "windows-latest", "macos-latest"]' >> $GITHUB_OUTPUT
          else
            echo 'matrix=["ubuntu-latest"]' >> $GITHUB_OUTPUT
          fi

  build:
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        os: ${{ fromJson(needs.prepare.outputs.matrix) }}
        include:
          - os: ubuntu-latest
            qt_arch: gcc_64
          - os: windows-latest
            qt_arch: win64_msvc2019_64
          - os: macos-latest
            qt_arch: clang_64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v3
        with:
          version: '6.5.*'
          arch: ${{ matrix.qt_arch }}

      - name: Install Dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1-mesa-dev

      - name: Configure
        run: |
          cmake -B build -S . -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release

      - name: Install Python test dependencies
        run: pip install Pillow

      - name: Test (Unix)
        if: runner.os != 'Windows'
        run: |
          export PATH="$PATH:$(pwd)/build"
          ctest --test-dir build --output-on-failure -C Release

      - name: Test (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $env:PATH = "$env:PATH;$(Get-Location)\build\Release;$env:Qt6_DIR\bin"
          ctest --test-dir build --output-on-failure -C Release

      - name: Package (Linux)
        if: runner.os == 'Linux' && github.ref == 'refs/heads/release'
        run: |
          mkdir dist
          cp build/sprat-gui dist/
          # Bundle sprat-cli (using local build tools is better but for now let's just create folders)
          mkdir -p dist/cli/transforms
          # Note: In a real CI this should probably download or have the CLI as a submodule/dependency
          # but here we follow the existing pattern of creating the structure.
          tar -czf sprat-gui-linux.tar.gz -C dist .

      - name: Package (Windows)
        if: runner.os == 'Windows' && github.ref == 'refs/heads/release'
        shell: pwsh
        run: |
          mkdir dist
          # Bundle sprat-cli (read version from DEPENDENCIES)
          $cli_version = (Get-Content DEPENDENCIES).Trim()
          $cli_url = "https://github.com/pedroac/sprat-cli/releases/download/$cli_version/sprat-cli-windows-x64.zip"
          Invoke-WebRequest -Uri $cli_url -OutFile cli.zip
          # Extract and preserve structure into dist/cli/
          Expand-Archive cli.zip -DestinationPath dist/cli -Force
          # Bundle GUI
          Copy-Item -Path build/Release/sprat-gui.exe -Destination dist/ -Force
          windeployqt dist/sprat-gui.exe
          Compress-Archive -Path dist/* -DestinationPath sprat-gui-windows.zip

      - name: Package (macOS)
        if: runner.os == 'macOS' && github.ref == 'refs/heads/release'
        run: |
          set -euo pipefail
          mkdir dist
          cp -r build/sprat-gui.app dist/
          # Bundle sprat-cli (read version from DEPENDENCIES) into the App Bundle
          cli_version=$(cat DEPENDENCIES | xargs)
          curl -fL -o cli.dmg "https://github.com/pedroac/sprat-cli/releases/download/$cli_version/sprat-cli-macos-x64.dmg"
          MOUNT_POINT=$(hdiutil mount cli.dmg | grep -o '/Volumes/.*' | head -n 1)
          if [ -z "$MOUNT_POINT" ]; then echo "Failed to mount DMG"; exit 1; fi
          cp "$MOUNT_POINT"/* dist/sprat-gui.app/Contents/MacOS/
          hdiutil unmount "$MOUNT_POINT"
          # Package DMG
          macdeployqt dist/sprat-gui.app -dmg
          mv dist/sprat-gui.dmg sprat-gui-macos.dmg

      - name: Upload Artifacts
        if: github.ref == 'refs/heads/release'
        uses: actions/upload-artifact@v4
        with:
          name: sprat-gui-${{ runner.os }}
          path: |
            sprat-gui-linux.tar.gz
            sprat-gui-windows.zip
            sprat-gui-macos.dmg

  build-wasm:
    if: >
      (github.event_name == 'push' && github.ref == 'refs/heads/release') ||
      github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    env:
      EM_CACHE: ${{ github.workspace }}/build_wasm/emsdk_cache

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Qt WASM
        uses: jurplel/install-qt-action@v3
        continue-on-error: true
        with:
          version: '6.6.3'
          arch: 'wasm_multithread'

      - name: Check Qt WASM availability
        id: check_qt_wasm
        run: |
          QT_WASM=$(find "$HOME/Qt" -maxdepth 3 -type d -name 'wasm_multithread' 2>/dev/null | head -1)
          if [ -z "$QT_WASM" ]; then
            echo "Qt WASM not found, skipping WASM build"
            echo "wasm_available=false" >> $GITHUB_OUTPUT
          else
            echo "Qt WASM found at: $QT_WASM"
            echo "wasm_available=true" >> $GITHUB_OUTPUT
            echo "qt_wasm_path=$QT_WASM" >> $GITHUB_OUTPUT
          fi

      - name: Setup Emscripten 4.0.7
        if: steps.check_qt_wasm.outputs.wasm_available == 'true'
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: '4.0.7'
          actions-cache-folder: 'emsdk-cache'

      - name: Build EGL stub
        if: steps.check_qt_wasm.outputs.wasm_available == 'true'
        run: |
          mkdir -p build_wasm/wasm_deps build_wasm/emsdk_cache/symbol_lists
          emcc wasm/egl_stub.c -c -o build_wasm/wasm_deps/egl_stub.o
          emar rcs build_wasm/wasm_deps/libegl.a build_wasm/wasm_deps/egl_stub.o

      - name: Configure WASM
        if: steps.check_qt_wasm.outputs.wasm_available == 'true'
        run: |
          QT_WASM="${{ steps.check_qt_wasm.outputs.qt_wasm_path }}"
          emcmake cmake -B build_wasm -S . \
            -DCMAKE_PREFIX_PATH="$QT_WASM" \
            -DCMAKE_FIND_ROOT_PATH="$QT_WASM" \
            -DCMAKE_MODULE_PATH="$QT_WASM/lib/cmake/Qt6" \
            -DSPRAT_WASM_DEBUG=OFF \
            -DSPRAT_WASM_JSPI=OFF \
            -DEGL_INCLUDE_DIR="$(em-config EMSCRIPTEN_ROOT)/system/include" \
            -DEGL_LIBRARY="$(pwd)/build_wasm/wasm_deps/libegl.a" \
            -DCMAKE_CXX_FLAGS="-DNDEBUG -DEMSCRIPTEN_DISABLE_ASSERTS=1" \
            -DCMAKE_C_FLAGS="-DNDEBUG -DEMSCRIPTEN_DISABLE_ASSERTS=1"

      - name: Build WASM
        if: steps.check_qt_wasm.outputs.wasm_available == 'true'
        run: |
          export EMFLAGS="-sASSERTIONS=0"
          cmake --build build_wasm --parallel

      - name: Package WASM
        if: steps.check_qt_wasm.outputs.wasm_available == 'true'
        run: |
          mkdir sprat-gui-web
          cp build_wasm/sprat-gui.{js,wasm,data} \
             build_wasm/qtloader.js \
             sprat-gui-web/
          cp build_wasm/sprat-gui.html sprat-gui-web/index.html
          zip -r sprat-gui-web.zip sprat-gui-web/

      - name: Upload WASM artifact
        if: steps.check_qt_wasm.outputs.wasm_available == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: sprat-gui-wasm
          path: sprat-gui-web.zip

      - name: WASM build skipped
        if: steps.check_qt_wasm.outputs.wasm_available != 'true'
        run: echo "⚠️ Qt WASM not available in aqtinstall, skipping WASM build"

  deploy-itchio:
    if: github.event_name == 'push' && github.ref == 'refs/heads/release'
    runs-on: ubuntu-latest
    needs: build-wasm

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read version
        id: version
        run: echo "tag=$(cat VERSION | xargs)" >> $GITHUB_OUTPUT

      - name: Download WASM artifact
        continue-on-error: true
        id: download_wasm
        uses: actions/download-artifact@v4
        with:
          name: sprat-gui-wasm
          path: dist

      - name: Check if WASM artifact exists
        id: check_wasm
        run: |
          if [ -f "dist/sprat-gui-web.zip" ]; then
            echo "wasm_exists=true" >> $GITHUB_OUTPUT
          else
            echo "wasm_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Extract WASM package
        if: steps.check_wasm.outputs.wasm_exists == 'true'
        run: unzip dist/sprat-gui-web.zip -d .

      - name: Download butler
        if: steps.check_wasm.outputs.wasm_exists == 'true'
        run: |
          curl -fsSL -o butler.zip \
            "https://broth.itch.ovh/butler/linux-amd64/LATEST/archive/default"
          unzip butler.zip
          chmod +x butler
          ./butler -V

      - name: Push to itch.io
        if: steps.check_wasm.outputs.wasm_exists == 'true'
        env:
          BUTLER_API_KEY: ${{ secrets.BUTLER_API_KEY }}
        run: |
          ./butler push sprat-gui-web/ pedroac/sprat-gui:web \
            --userversion ${{ steps.version.outputs.tag }}

      - name: itch.io deployment skipped
        if: steps.check_wasm.outputs.wasm_exists != 'true'
        run: echo "⚠️ WASM build not available, itch.io deployment skipped"

  release:
    if: github.event_name == 'push' && github.ref == 'refs/heads/release'
    runs-on: ubuntu-latest
    needs: [build, build-wasm]
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Read version
        id: version
        run: |
          VERSION=$(cat VERSION | xargs)
          echo "tag=$VERSION" >> $GITHUB_OUTPUT

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: Release ${{ steps.version.outputs.tag }}
          files: |
            dist/sprat-gui-linux.tar.gz
            dist/sprat-gui-windows.zip
            dist/sprat-gui-macos.dmg
            dist/sprat-gui-web.zip
          draft: false
          prerelease: false
